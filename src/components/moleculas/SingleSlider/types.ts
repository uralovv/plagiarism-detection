export namespace SingleSliderTypes {
  export interface IProps {
    items: any;
    className?: string;
  }
  export interface MealTypes {
    title: string;
    structure: string;
    price: number;
    img: string;
  }
}
